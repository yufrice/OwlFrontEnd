image: node:jessie

cache:
  paths:
  - node_modules/

pages:
  stage: deploy
  script:
    - yarn install
    - yarn run build-storybook
    - rm -rf public
    - mkdir public
    - mv storybook-static/* public/
  artifacts:
    paths:
    - public